// You can run scripts with the truffle console using:
//      truffle exec [script/path]
// You must use the following exports template in order for these scripts to
// run.
module.exports = function(callback) {
  // perform actions
}
